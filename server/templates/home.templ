package templates

import (
    "github.com/sirrobot01/lamba/server/components"
    "github.com/sirrobot01/lamba/pkg/executor"
)

templ Home(ex *executor.Executor) {
    @components.Layout() {
        @components.FunctionList(ex)
    }
}